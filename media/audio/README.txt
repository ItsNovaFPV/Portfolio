Drop these audio files into this folder before the interview.
Filenames must match exactly (case-sensitive) — the lesson page expects them here.

    Organ.mp3              ← Santana "Oye Como Va" stem — organ (carries the bell pattern)
    Bass.mp3               ← Bass stem
    Guitar.mp3             ← Lead guitar stem
    Percussion.mp3         ← Percussion stem
    oye-como-va-full.mp3   ← The complete song (drop this in when ready)

Notes:
  - The 4 stems should be the same length so they loop together cleanly.
  - MP3 is preferred for broad browser support. M4A / OGG also work; if you use a
    different extension, also update the filenames inside greenteacher/index.html
    (search for "stemFiles =" near the top of the <script> block).
  - Nothing else in this folder is required.
